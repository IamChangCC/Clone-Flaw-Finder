基于代码属性图的基本块提取算法：
1.获取所有函数的functionId,依次对每个函数处理，提取其基本块信息
2.提取CFG 子图，起点是ENTRY节点，广度优先遍历该CFG
3.CFG中每个节点以CONTROLS边到达的子节点都属于同一基本块，但其先后顺序需要依据POST_DOM边进行排序
4.得到的各函数基本块对应的node_id后，提取其code内容，以同样顺序即可得到各基本块对应的代码
5以函数为单位，将各基本块代码保存在以该函数function_id为文件名的文档中。
